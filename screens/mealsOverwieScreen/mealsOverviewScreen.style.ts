import { StyleSheet } from 'react-native';
import * as N from 'react-native';

const getStyles = () => {
  return StyleSheet.create({
    wrap: {
      flex: 1,
      padding: 16,
    },
  });
};

export { getStyles };
